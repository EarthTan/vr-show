{
  "name": "vr-show",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "dist:mac": "tauri build && bash scripts/copy-bundles.sh macos",
    "dist:win": "tauri build && bash scripts/copy-bundles.sh windows",
    "dist:linux": "tauri build && bash scripts/copy-bundles.sh linux"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.3",
    "happy-dom": "^15.0.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
